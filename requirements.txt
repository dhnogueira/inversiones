# ============================================================
# PicadoFino — Python Dependencies
# ============================================================
# Instalar con:  pip install -r requirements.txt
#
# Categorías:
#   [BACKEND]   FastAPI, Django, SQLAlchemy, Pydantic, Celery
#   [FINANCE]   yfinance, pandas, numpy, ta, openbb, qlib
# ============================================================

# ─── Python Backend ─────────────────────────────────────────
# 1. FastAPI — High-performance async REST API framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0           # ASGI server for FastAPI

# 2. Django — Full-featured web framework
django>=5.1.0
djangorestframework>=3.15.0         # DRF for REST APIs
django-cors-headers>=4.4.0          # CORS support

# 3. SQLAlchemy — Flexible Python ORM
sqlalchemy>=2.0.0
alembic>=1.13.0                     # Schema migrations for SQLAlchemy
asyncpg>=0.29.0                     # Async PostgreSQL driver
aiosqlite>=0.20.0                   # Async SQLite driver (dev/tests)
psycopg2-binary>=2.9.0              # Sync PostgreSQL driver

# 4. Pydantic — Data validation & settings management
pydantic>=2.9.0
pydantic-settings>=2.5.0            # .env file support for settings

# 5. Celery — Distributed task queue
celery[redis]>=5.4.0
redis>=5.0.0                        # Redis broker + result backend
flower>=2.0.0                       # Celery monitoring UI

# ─── General Backend Utilities ───────────────────────────────
httpx>=0.27.0                       # Async HTTP client (FastAPI testing)
python-dotenv>=1.0.0                # .env file loader
python-jose[cryptography]>=3.3.0   # JWT authentication
passlib[bcrypt]>=1.7.4             # Password hashing
python-multipart>=0.0.12            # File upload support (FastAPI)

# ─── Financial Analysis ─────────────────────────────────────
# 1. yfinance — Yahoo Finance market data (FREE)
yfinance>=0.2.40

# 2. pandas + numpy — Core data manipulation
pandas>=2.2.0
numpy>=1.26.0

# 3. OpenBB — Open-source Bloomberg alternative
openbb>=4.3.0

# 4. ta — Technical Analysis indicators (70+ indicators)
ta>=0.11.0

# 5. Qlib (Microsoft) — AI-oriented quantitative investment platform
# Note: Qlib requires extra setup. Install separately if needed:
# pip install pyqlib
# qlib requires C++ build tools on Windows
# pyqlib>=0.9.0

# ─── Finance Extras ──────────────────────────────────────────
matplotlib>=3.9.0                   # Financial charting
plotly>=5.24.0                      # Interactive charts
mplfinance>=0.12.10b0              # Candlestick charts
scikit-learn>=1.5.0                 # ML for quantitative strategies
scipy>=1.13.0                       # Statistical analysis
statsmodels>=0.14.0                 # Time series models (ARIMA, etc.)

# ─── Dev & Testing ───────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0                       # For FastAPI TestClient
ruff>=0.6.0                         # Fast Python linter
black>=24.0.0                       # Code formatter
